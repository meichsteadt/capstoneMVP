<h3>Edit Landing Page</h3>
<%= form_for(@layout) do |f| %>
  <%= render 'errors', object: @layout %>
  <div class="form-group">
    <%= f.label :store_image %>
    <%= f.file_field :store_image %>
  </div>
  <div class="form-group">
    <%= f.label :store_copy %>
    <%= f.text_area :store_copy, class: 'form-control', rows: 8 %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-default'%>
  </div>
<% end %>
