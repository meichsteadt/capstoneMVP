<div class="container">
  <div class="row">
    <div class="box promo">
      <div class="col-lg-12">
        <hr>
        <h2 class="intro-text text-center">
            <strong><%= @product_type %></strong>
        </h2>
        <hr>
      </div>
      <div class="col-lg-12 text-center">
          <p>At <%= @store.name %> we are always running into great deals on products. And we think it's our obligation to pass those savings on to our customers! Come look at our lineup of current promotional products.</p>
      </div>
      <% if @products.length > 0 %>
        <% @products.each do |product| %>
          <div class="col-md-4 text-center">
              <%= link_to(product_path(product)) do %><img class="img img-responsive shadow" src="<%= product.image %>" alt=""><% end %>
              <h2><%= product.price %></h2>
              <p><%= product.name %></p>
          </div>
        <% end %>
      <% else %>
        <h3>Unfortunately, there's no products in this category yet.</h3>
      <% end %>
      <div class="clearfix"></div>
    </div>
  </div>
</div>
